import { normalizePixel } from '@/theme'

export const SIZES = {
	S10: normalizePixel(10),
	S11: normalizePixel(11),
	S12: normalizePixel(12),
	S13: normalizePixel(13),
	S14: normalizePixel(14),
	S16: normalizePixel(16),
	S17: normalizePixel(17),
	S18: normalizePixel(18),
	S20: normalizePixel(20),
	S22: normalizePixel(22),
	S24: normalizePixel(24),
	S28: normalizePixel(28),
}
